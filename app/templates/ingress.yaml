apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: issue-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: ImplementationSpecific
        backend:
          service:
            name: testapp
            port:
              number: 80
